{% extends 'baseAdmin.html.twig' %}

{% block title %}Video{% endblock %}

{% block body %}
    <h1>Video</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>IdV</th>
            <td>{{ video.idV }}</td>
        </tr>
        <tr>
            <th>Titre</th>
            <td>{{ video.titre }}</td>
        </tr>
        <tr>
            <th>Genre</th>
            <td>{{ video.genre }}</td>
        </tr>
        <tr>
            <th>Videopath</th>
            <td>{{ video.videopath }}</td>
        </tr>
        <tr>
            <th>NbLikes</th>
            <td>{{ video.nbLikes }}</td>
        </tr>
        <tr>
            <th>NbDislikes</th>
            <td>{{ video.nbDislikes }}</td>
        </tr>
        <tr>
            <th>Mailsent</th>
            <td>{{ video.mailsent }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('video_back_index') }}">back to list</a>

    <a href="{{ path('video_back_edit', {'idV': video.idV}) }}">edit</a>

    {{ include('video_back/_delete_form.html.twig') }}
    <a href="{{ path('pdfShow',{'idV': video.idV}) }}" class="btn bg-purple btn-flat"><i clas = "fa fa-file-pdf-o"></i>PDF</a>
{% endblock %}
